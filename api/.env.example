# Turso (libSQL) — same DB n8n writes to
TURSO_DATABASE_URL=libsql://n8n-finances-<org>.turso.io
TURSO_AUTH_TOKEN=eyJ...

# Bearer token the dash / LLM must send:  Authorization: Bearer <API_TOKEN>
API_TOKEN=change-me-long-random

# Optional
PORT=8080
RUST_LOG=info
